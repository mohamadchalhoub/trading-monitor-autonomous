/** DI token for the selected `AutonomousAiProvider` implementation — same convention as `ai/ai-provider.token.ts`, kept separate since it's a different interface entirely. */
export const AUTONOMOUS_AI_PROVIDER = Symbol('AUTONOMOUS_AI_PROVIDER');
